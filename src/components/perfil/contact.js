const Contact = ({ theme, style }) => {
  return (
    <div>
      <div className={style} style={{ backgroundColor: theme }}>
        <span>Guardar Contacto</span>
      </div>
    </div>
  );
};
export default Contact;
